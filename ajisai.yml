workspace:
  imports:
    local_rules:
      type: local
      path: "./.ai"
      include:
      - local_rules_default
    remote_rules:
      type: git
      repository: https://github.com/sushichan044/ai-presets.git
      include:
      - example1
  integrations:
    cursor:
      enabled: true
    github-copilot:
      enabled: true
    windsurf:
      enabled: true
